#include "def.h"

void print_green() {
    if (!isRe && !isPi)
        printf(GREEN);
}

void print_yellow() {
    if (!isRe && !isPi)
        printf(YELLOW);
}

void print_reset() {
    if (!isRe && !isPi)
        printf(NORMAL);
}

void print_red() {
    if (!isRe && !isPi)
        printf(RED);
}

void print_cyan() {
    if (!isRe && !isPi)
        printf(CYAN);
}



